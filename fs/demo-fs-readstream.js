const fs = require('fs');

// const readable = fs.createReadStream('./demo-fs-stats.js');
// readable.pipe(process.stdout);

//
// readable.on('readable', () => {
//   console.log('stread is ready to read.');
// })
// readable.on('data', (chunk) => {
//   console.log(`${chunk}`);
// })
// readable.on('end', () => {
//   console.log('stream is end.');
// })
// readable.on('close', () => {
//   console.log('stream is closed.');
// })
